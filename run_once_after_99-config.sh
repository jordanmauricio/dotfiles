# setup node env
# configure github --> look into using a template to hide email adres and such
# finish setting up 1password by going to Settings > Developer and enable CLI
# configure Finder settings
# copy ssh private key over
